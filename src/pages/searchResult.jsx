import React from 'react'

function searchResult(props) {
    const keys = ["title", "type", "price", "brand", "description", "delivery", "pickup", "status"]
  return (
    <div>searchResult</div>
  )
}

export default searchResult